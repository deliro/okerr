---
coverage:
  precision: 2
  round: nearest
  range: "80...100"
  status:
    project:
      default:
        target: 85%
        threshold: 3%
    patch:
      default:
        target: 100%
        threshold: 5%
comment:
  layout: "diff, flags, files"
  behavior: default
  require_changes: true
